# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Render sets DATABASE_URL, RENDER=true, RENDER_EXTERNAL_HOSTNAME automatically
# DATABASE_URL=postgres://...

# Custom domain on Render (forms/login need these):
# CSRF_TRUSTED_ORIGINS=https://pcchecker.gilliomfrontlinedigital.com
# CUSTOM_DOMAINS=pcchecker.gilliomfrontlinedigital.com

# AI diagnostics — https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini

# Optional API key for /api/scan/ endpoints (header X-API-Key)
PCC_API_KEY=
